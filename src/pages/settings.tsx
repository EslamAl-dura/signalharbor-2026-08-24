import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '../components/ui/card'
import { Select } from '../components/ui/select'
import { Badge } from '../components/ui/badge'
import { Layout } from '../components/layout'
import { useI18n } from '../providers/i18n-provider'
import { useTheme } from '../providers/theme-provider'

export function Settings() { const { t, locale, setLocale } = useI18n(); const { theme, setTheme } = useTheme(); return <Layout><section className="mb-8 pt-8"><Badge>{t('workspace')} · Acme Platform</Badge><h1 className="mt-4 text-3xl font-bold">{t('settings')}</h1><p className="mt-2 text-slate-500 dark:text-slate-400">Personalize how SignalHarbor communicates release confidence.</p></section><div className="grid max-w-3xl gap-5"><Card><CardHeader><CardTitle>{t('appearance')}</CardTitle><CardDescription>Choose the visual mode for your operations desk.</CardDescription></CardHeader><CardContent><label className="block text-sm font-medium">{theme === 'dark' ? t('dark') : t('light')}<Select className="mt-2" value={theme} onChange={e => setTheme(e.target.value as 'light' | 'dark')}><option value="dark">{t('dark')}</option><option value="light">{t('light')}</option></Select></label></CardContent></Card><Card><CardHeader><CardTitle>{t('language')}</CardTitle><CardDescription>Arabic automatically switches the interface to right-to-left reading order.</CardDescription></CardHeader><CardContent><label className="block text-sm font-medium">{locale === 'en' ? t('english') : t('arabic')}<Select className="mt-2" value={locale} onChange={e => setLocale(e.target.value as 'en' | 'ar')}><option value="en">{t('english')}</option><option value="ar">{t('arabic')}</option></Select></label></CardContent></Card></div></Layout> }